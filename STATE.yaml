state_version: 3
last_update: '2026-08-14T14:30:00'

# THE authoritative per-layer live state. Kept in sync with lib/ + scripts/ + experiments.
# Status: DONE | PARTIAL | IN_PROGRESS | NOT_STARTED | BLOCKED
# Each layer maps to VISION-CHUNK-LAYER-MAP.md chunk N.
# Source of truth for "what's actually built": data/references/experiments.json + lib/*.py

layers:
  00-core-engine:
    chunk: 1
    status: DONE
    note: 'lib/epistemic.py (ladder + 4-axis Authority + invariant) + lib/schema.py (single-source
      compiler). Envelope applied to 490 nodes/6578 edges; invariant 0 violations; eigenius-grade
      mapping order-preserving.'
  01-corpus-provenance:
    chunk: 2
    status: DONE
    note: '425 clean docs, corpus.jsonl versioned, R2 backup. import_scifact generalization adapter
      validated (external claim enters engine).'
  02-epistemic-graph:
    chunk: 3
    status: DONE
    note: '490 nodes/6578 edges with envelopes. Canonical DAG validated. Communities match epistemic
      split. Provenance nanopub emission (knowledgeProvenance) validated. Certification-weight kernel.'
  03-factory:
    chunk: 4
    status: DONE
    note: 'lib/staleness.py (RKA blast-radius + review_queue + rebuild order) + canonical-dag.yaml.
      Counterfactual engine: THERMODYNAMICS most load-bearing (11 downstream).'
  04-argument-engine:
    chunk: 5
    status: DONE
    note: 'lib/review.py (herdr reducer) + argument.json (AIF 6/4/2). Crux compiler: indeterminism
      necessity is the minimal divergence. Rival-argument: justified wins.'
  05-review-gate:
    chunk: 6
    status: DONE
    note: 'lib/scholar_review.py (adversarial panel + cross-review + CiteCheck) + review.py reducer.
      Mutation-testing 100% kill-rate (self-auditing verifier). Self-improvement as PR.'
  06-retrieval-compiler:
    chunk: 7
    status: DONE
    note: 'lib/retrieval.py (PathRAG + HippoRAG) + lib/query.py (KG2Code executable DSL). Layer 10
      comparison validated (PathRAG+KG2Code win; HippoRAG hub-bias found).'
  07-surfaces:
    chunk: 8
    status: NOT_STARTED
    note: 'Astro site + API + MCP (SPEC-00/09). mcp-agent + agent-kit cloned as reference.'
  08-domain-expansions:
    chunk: 9
    status: PARTIAL
    note: 'Generalization validated (engine core domain-agnostic: Doyle + SciFact + EleutherIA).
      science/ philosophy/ sanskrit/ ontology extensions not yet built.'
  09-live-system:
    chunk: 10
    status: PARTIAL
    note: 'Vision->chunk->layer map + STATE.yaml + specs + AGENTS live. Reactive-essay + signed
      corpus root (Merkle) + graphiti-temporal validated. Education + organism kernels built.'

# beyond-patala product mechanisms (new, coded in lib/)
product_kernels:
  certificate.py:
    status: DONE
    note: 'Certification Weight (marketplace): compounding verification metric; CW 36->1683 over 10yr.'
  discovery.py:
    status: DONE
    note: 'Research Value Score (what-if machine): load-bearing x unverified x crux pressure.'
  education.py:
    status: DONE
    note: 'LearningClaim + interaction compiler + wrong-answer->neighbor (education moat).'
  organism.py:
    status: DONE
    note: 'UserKnowledgeState + MisconceptionGraph (the learner sensor / flywheel).'
