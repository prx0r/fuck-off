# THE ULTIMATE OPTIMIZED PRODUCT — the v3 organism, assembled on the real IPVV corpus

*2026-08-14. Inspired by patala's new `migration/v3/` (the ORGANISM), and tested on the REAL IPK/IPVV
corpus with the Ratié commentary. This is the ultimate optimized product: the full v3 product stack
assembled from the 17 proven kernels, one claim at a time, with honest statuses and the graduation
proof on the actual target corpus.*

---

## The v3 insight we adopted (from patala migration/v3)

v3 reframes the system from a *pipeline* into a **single organism with 5 organ-systems**:
1. **Nervous** — Hermes (execution)
2. **Skeleton** — the kernel + one derivation graph (truth)
3. **Digestive** — the reactive factory (transformation)
4. **Reproductive** — the production organism (essay → render → publish)
5. **Sensory** — the consumer read-plane + organism (reception) — *"the loop that makes it an
   organism, not a database."*

The three truths: **Pāṭala decides** what can responsibly be said (the epistemic gate) · **the
Library** decides what is worth communicating · **Renderio** decides how it should be seen.

**The product logic we built on:** "Reuse, never rebuild." The 17 kernels ARE the product machinery.
Every product is a **projection** of the verified graph + kernels. We proved 13/16 products; the two
strongest are TranslationProof and Education; 3 need building (Essay, Commentary, Tokenization).

---

## What we built (tested on the REAL corpus)

### 1. The IPVV graduation — `validate-graduation-ipvv.py` (18/18 on real IPK)

The Doyle graduation (14/14) proved the mechanism; THIS proves it on the ACTUAL target. A real IPK
claim through the whole organism:

> **IPK 1.5.19**: determinate cognition (adhyavasāya) is "the very power of the supreme Lord,
> manifested in the same way as the self" — the felt→ground one-support step. Ratié reads this felt
> recognition as camatkāra (Ch7), with vimarśa defined at 1.5.11/1.5.13.

**Chain (real data):** real Torella IPK text → honest envelope (IPK claim = SCHOLARLY_CORROBORATED,
felt→ground universal reading = MACHINE_PROPOSED) → adversarial review (addition held in CORRECTION)
→ **MUTATE premise 1.5.11** → staleness blast-radius → Ratié essay prose stale → pedagogy re-examines
learner → organism records the felt→ground confusion → signed re-release → invariant holds. **18/18.**

### 2. The ultimate product stack — `lib/patala_product.py` + `validate-product-stack.py` (13/13)

The full v3 product stack assembled from ALL 17 kernels, for one real IPK claim:

| Family | Products | Kernels (reused) |
|--------|----------|------------------|
| **TEXTS** | Translation · TranslationProof (the moat) · Passage | `translation.py`, `query.py` |
| **ARGUMENTS** | Claim · Argument · Crux · Comparison · Synthesis | `epistemic.py`, `review.py`, `crux-compiler`, `claim-standardisation`, `evolve.py` |
| **SCHOLAR** | ResearchPacket · Review · ScholarAttestation · Audit · Benchmark | `retrieval.py`, `scholar_review.py`, `agent_delivery.py`, `theatre-check`, `import-scifact` |
| **LEARN** | Essay · Explainer · ArgumentMap · UnderstandingCheck · Course | `essay_ingest.py`, `education.py`, `pedagogy.py` |

**18 products from one claim, all projections of the 17 proven kernels.** The TranslationProof moat is
the non-aggregate 11-dim vector (gate BLOCKS on any failing dim — never a scalar). Certification Weight
(compounding economics) + Research Value (What-If signal) power the expansions. **13/13.**

---

## Why this is "the ultimate optimized product"

1. **Reuse, never rebuild.** Nothing re-implemented — the product kernel `patala_product.py` is a thin
   *assembly* that calls the proven kernels. This is v3's central discipline, made literal.
2. **Honest statuses.** Every product reports PROVEN / PROVEN-MECHANISM — no theatre. The
   ScholarAttestation honestly flags gap E (signed attestation).
3. **The moat is real.** TranslationProof is a non-aggregate vector of obligations, gate BLOCKS on any
   failing dimension — proof≠policy, exactly v3's strongest product.
4. **Tested on the actual corpus.** Not the Doyle stand-in — the real Torella IPK text with the real
   Ratié commentary and the genuine felt→ground crux.
5. **The organism loop is present.** The learner's felt→ground confusion becomes a research signal
   (sensory system → back into the graph). This is what makes it an organism, not a database.

---

## What to build next (from the honest gap)

- **The 3 v3 needs-build products:** Essay projection, Commentary, Tokenization (Text-Fabric + Vidyut).
- **Signed human attestation (gap E):** replace plain `human_authorize()` with a real cosign-style
  signed HumanAttestation — before any marketplace/public authority.
- **Bulk gold ingest:** register the 63 L200 + 63 C1 golds with derivation edges (the highest-leverage
  next build per v2's plan).
- **Wide the stack:** run `validate-product-stack.py` over MANY IPK claims (a real IPVV corpus pass),
  then feed the produced essays/lessons through the real organism loop.

## Proofs
- `scripts/validate-graduation-ipvv.py` — 18/18 on real IPK.
- `scripts/validate-product-stack.py` — 13/13 (18 products, 4 families).
- `lib/patala_product.py` — the product kernel (assembles all 17 kernels).
