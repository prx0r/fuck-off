# LAYER 08 — DOMAIN EXPANSIONS
*Spine (Chunk 9). Domain-specific ontology extensions over the engine core.*

## 1. What it is
The domain subclasses that live OUTSIDE the core — science/, philosophy/, sanskrit/. This is what
makes the engine general (VISION.md §engine/domain separation).

## 2. Purpose
Prove the engine generalizes: same kernel, different ontology extension. If Bell + Dennett + Shannon
all fit, the abstraction is real.

## 3. Data
- `science/`: Experiment · Dataset · Method · Measurement · Replication
- `philosophy/`: Position · Objection · ThoughtExperiment · ArgumentForm
- `sanskrit/`: Manuscript · Reading · Translation · Commentary

## 4. Processes
```
core envelope → add domain subclasses → run engine unchanged
```

## 5. Implementations
- `docs/vision/VISION.md` (engine/domain separation)

## 6. Docs
- `docs/vision/VISION.md`

## 7. Current state
`EMPTY` (see `STATE.yaml`). `lib/domains/` does not exist yet — the domain-expansion plane is unbuilt.
Do not over-claim VALIDATED here.
