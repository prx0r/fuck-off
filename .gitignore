# node_modules — not committed
web/node_modules/

# astro build output
web/dist/

# the compiled static site is regenerated by build-static-site.py — commit site/ but not transient
site/.astro/

# python bytecode — never commit
__pycache__/
*.pyc

# local deploy cache (not source)
edge/.wrangler/
