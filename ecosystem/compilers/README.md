# compilers/ — graph-as-compile-output clones

Repos that treat the graph as a compiled representation of documents. See `../../docs/ECOSYSTEM-INDEX.md` §1.

| Repo | Why we cloned |
|------|---------------|
| graphify (rhanka/graphify) | extract→canonicalize→reconcile→typed graph |
| sage-wiki (xoai/sage-wiki) | graph as compile output (validates SPEC-00) |
| obra/knowledge-graph | query a vault as a KG; agent interface |
